relationships:
  original: {en: The original raw content, zh: 原始原始内容}
  success: {en: Any FlowFile that is successfully parsed as a netflowv5 data will
      be transferred to this Relationship., zh: 任何成功解析为netflowv5数据的FlowFile都将传输到此关系。}
  failure: {en: Any FlowFile that could not be parsed as a netflowv5 message will
      be transferred to this Relationship without any attributes being added, zh: 无法解析为netflowv5消息的任何FlowFile都将传输到此关系，而不添加任何属性}
capabilityDescription: {en: Parses netflowv5 byte ingest and add to NiFi flowfile
    as attributes or JSON content., zh: 分析netflowv5字节摄取，并将其作为属性或JSON内容添加到NiFi流文件中。}
readAttributes:
  udp.port: {en: Optionally read if packets are received from UDP datagrams., zh: 如果从UDP数据报接收到数据包，则可选读取。}
properties:
  FIELDS_DESTINATION:
    en: {displayName: Parsed fields destination, description: 'Indicates whether the
        results of the parser are written to the FlowFile content or a FlowFile attribute;
        if using flowfile-attribute, fields will be populated as attributes. If set
        to flowfile-content, the netflowv5 field will be converted into a flat JSON
        object.'}
    zh: {description: 指示解析器的结果是写入FlowFile内容还是写入FlowFile属性；如果使用flowfile属性，则字段将作为属性填充。如果设置为flowfile内容，netflowv5字段将转换为一个平面JSON对象。,
      displayName: 解析字段目标}
writeAttributes:
  netflowv5.header.*: {en: The key and value generated by the parsing of the header
      fields., zh: 解析头字段生成的键和值。}
  netflowv5.record.*: {en: The key and value generated by the parsing of the record
      fields., zh: 解析记录字段生成的键和值。}
tags:
  en: [network, netflow, attributes, datagram, v5, packet, byte]
  zh: [网络, Netflow, 属性, 数据报, 第5版, 小包裹, 字节]
