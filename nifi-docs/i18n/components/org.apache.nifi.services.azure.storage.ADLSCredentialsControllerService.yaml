capabilityDescription: {en: Defines credentials for ADLS processors., zh: 定义ADLS处理器的凭据。}
properties:
  storage-account-name:
    en: {displayName: Storage Account Name, description: 'The storage account name.
        There are certain risks in allowing the account name to be stored as a flowfile
        attribute. While it does provide for a more flexible flow by allowing the
        account name to be fetched dynamically from a flowfile attribute, care must
        be taken to restrict access to the event provenance data (e.g., by strictly
        controlling the policies governing provenance for this processor). In addition,
        the provenance repositories may be put on encrypted disk partitions.'}
    zh: {description: 存储帐户名。允许将帐户名存储为流文件属性存在一定风险。虽然它允许从流文件属性动态获取帐户名，从而提供了更灵活的流，但必须注意限制对事件起源数据的访问（例如，通过严格控制控制此处理器起源的策略）。此外，源存储库可以放在加密的磁盘分区上。,
      displayName: 存储帐户名称}
  service-principal-client-secret:
    en: {displayName: Service Principal Client Secret, description: Password of the
        Client/Application. The property is required when Service Principal authentication
        is used.}
    zh: {description: 客户端/应用程序的密码。使用服务主体身份验证时需要该属性。, displayName: 服务主体客户端机密}
  storage-sas-token:
    en: {displayName: SAS Token, description: 'Shared Access Signature token, including
        the leading ''?''. Specify either SAS token (recommended) or Account Key.
        There are certain risks in allowing the SAS token to be stored as a flowfile
        attribute. While it does provide for a more flexible flow by allowing the
        SAS token to be fetched dynamically from a flowfile attribute, care must be
        taken to restrict access to the event provenance data (e.g., by strictly controlling
        the policies governing provenance for this processor). In addition, the provenance
        repositories may be put on encrypted disk partitions.'}
    zh: {description: 共享访问签名令牌，包括前导“？”。指定SAS令牌（推荐）或帐户密钥。允许将SAS令牌存储为流文件属性存在一定风险。虽然它通过允许从流文件属性动态获取SAS令牌提供了更灵活的流，但必须注意限制对事件起源数据的访问（例如，通过严格控制控制该处理器起源的策略）。此外，源存储库可以放在加密的磁盘分区上。,
      displayName: SAS令牌}
  storage-use-managed-identity:
    en: {displayName: Use Azure Managed Identity, description: Choose whether or not
        to use the managed identity of Azure VM/VMSS}
    zh: {description: 选择是否使用Azure VM/VMSS的托管标识, displayName: 使用Azure托管身份}
  storage-endpoint-suffix:
    en: {displayName: Endpoint Suffix, description: Storage accounts in public Azure
        always use a common FQDN suffix. Override this endpoint suffix with a different
        suffix in certain circumstances (like Azure Stack or non-public Azure regions).}
    zh: {description: 公共Azure中的存储帐户始终使用公共FQDN后缀。在某些情况下（如Azure Stack或非公共Azure区域），使用不同的后缀覆盖此端点后缀。,
      displayName: 端点后缀}
  storage-account-key:
    en: {displayName: Storage Account Key, description: 'The storage account key.
        This is an admin-like password providing access to every container in this
        account. It is recommended one uses Shared Access Signature (SAS) token instead
        for fine-grained control with policies. There are certain risks in allowing
        the account key to be stored as a flowfile attribute. While it does provide
        for a more flexible flow by allowing the account key to be fetched dynamically
        from a flowfile attribute, care must be taken to restrict access to the event
        provenance data (e.g., by strictly controlling the policies governing provenance
        for this processor). In addition, the provenance repositories may be put on
        encrypted disk partitions.'}
    zh: {description: 存储帐户密钥。这是一个类似管理员的密码，可以访问此帐户中的每个容器。建议使用共享访问签名（SAS）令牌代替策略的细粒度控制。允许将帐户密钥存储为流文件属性存在一定风险。虽然它通过允许从流文件属性动态获取帐户密钥来提供更灵活的流，但必须注意限制对事件起源数据的访问（例如，通过严格控制控制该处理器起源的策略）。此外，源存储库可以放在加密的磁盘分区上。,
      displayName: 存储帐户密钥}
  service-principal-tenant-id:
    en: {displayName: Service Principal Tenant ID, description: Tenant ID of the Azure
        Active Directory hosting the Service Principal. The property is required when
        Service Principal authentication is used.}
    zh: {description: 托管服务主体的Azure Active Directory的租户ID。使用服务主体身份验证时需要该属性。, displayName: 服务主要租户ID}
  service-principal-client-id:
    en: {displayName: Service Principal Client ID, description: Client ID (or Application
        ID) of the Client/Application having the Service Principal. The property is
        required when Service Principal authentication is used.}
    zh: {description: 具有服务主体的客户端/应用程序的客户端ID（或应用程序ID）。使用服务主体身份验证时需要该属性。, displayName: 服务主体客户端ID}
  managed-identity-client-id:
    en: {displayName: Managed Identity Client ID, description: Client ID of the managed
        identity. The property is required when User Assigned Managed Identity is
        used for authentication. It must be empty in case of System Assigned Managed
        Identity.}
    zh: {description: 托管标识的客户端ID。当用户分配的托管标识用于身份验证时，该属性是必需的。如果是系统分配的管理标识，则必须为空。, displayName: 托管身份客户端ID}
tags:
  en: [azure, microsoft, cloud, storage, adls, credentials]
  zh: [蔚蓝色的, 微软, 云, 存储, 广告语, 资格证书]
